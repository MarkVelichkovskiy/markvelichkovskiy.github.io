
function ageChecker() {
    const name = prompt('What is your name?');
    const age = +prompt('How old are You?');
    const n = prompt('What is your name?');
    const a = +prompt('How old are You?');
    console.log(n);
    console.log(a);

    if (age < 18) {


        if (age < 18) {
            confirm('You are not allowed to visit this website!');
        }
            if (age >= 18) {
            if (age >= 18 || age < 21) {
                let b = confirm("Are you sure you want to continue?");
            if (c === true) {
                confirm('Welcome ' + name);
                confirm('Welcome, ' + b + '!!!');
            }
        
        else {
            confirm('You are not allowed to visit this website!');
        }
    }
    if (age > 21) {
        alert(name + ' welcome!');
        }
        }
    }
}
ageChecker(); 
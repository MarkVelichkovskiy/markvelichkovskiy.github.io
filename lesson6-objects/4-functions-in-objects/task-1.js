/**
 * Створити об'єкт телефону з властивостями:
 * назва - iPhone, модель - 12, ціна - 1000, залишок на складі - 8
 * 
 * Додати функцію для обчислення сумарної вартості продукту на складі
 * 
 * - вивести в консоль сумарну вартість
 * - дати користувачу змогу змінити наявність на складі через модальне вікно
 * - вивести в консоль сумарну вартість
 * 
 */

const phone = {
    brand: 'iPhone',
    model: '12',
    price: '1000',
    store: 'end',
}

 function creatauto (brand, model, age)
{
    const user = {
        firstname: firstname,
        lastname: lastname,
        age: age,
        fullname: firtname+ " "+lastname, 
    }

    return user;
}

let user1 = createUser('Rayn', '')